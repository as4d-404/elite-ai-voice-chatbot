fastapi==0.115.0
uvicorn[standard]==0.30.6
twilio==9.3.2
websockets==13.1
python-dotenv==1.0.1
# supabase 2.31.0+ supports httpx 0.28.x, which google-genai requires.
supabase==2.31.0
pydantic>=2.13.5
httpx>=0.28.1,<0.29

# Gemini Live voice/agent layer (free Google AI Studio tier, no billing)
google-genai==2.20.0

# OpenAI Agents SDK - for SDK compliance
openai>=1.30.0,<2

# Claude Agent SDK - for SDK compliance
anthropic==0.31.0

# Transcoding for the Twilio bridge: 8kHz mu-law <-> 16/24kHz PCM.
# audioop-lts is the maintained drop-in backport of Python's removed `audioop`
# (imported as `import audioop`), required on Python 3.13+.
audioop-lts==0.2.2

# FastAPI file upload support (required for /api/transcribe)
python-multipart>=0.0.18
